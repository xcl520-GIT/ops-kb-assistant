# 统一换行符，避免不同系统上 diff 全是整文件变更
* text=auto eol=lf

# Windows 批处理必须用 CRLF，否则部分环境下会执行异常
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 二进制
*.png binary
*.jpg binary
*.ico binary
*.db binary
